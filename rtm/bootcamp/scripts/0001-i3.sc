0 i3 is the window manager
0.1 Although you are in a terminal, the system is running in a graphical mode
0.1 The GUI - graphical mode - is too slow for the raspberry pi
0.1 You can run some things, including browsers, but the experience is not ok
0.1 The shortcut key for i3 is "super" - the one between fn and alt
0.1 User super + 2 (or any number) to jump to a different workspace
0.1 devkit's terminal is in workspace 1
0.1 To toggle full screen, use super + f
0.1 To exit i3 use super + e (mouse needed)
