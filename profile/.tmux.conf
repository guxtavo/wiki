set -g status-right-length 200
set-option -g status-position top
set -g status-right '#(cd #{pane_current_path}; git rev-parse --abbrev-ref HEAD) #(cd #{pane_current_path}; git status --porcelain | wc -l) | #(~/git/wiki/profile/git-tmux.sh) | #(~/git/wiki/profile/tmux_tasks.sh) | #(~/git/wiki/profile/weather.sh) | #(sensors | grep C | cut -b 15-17 | uniq | tail -2 | tr "\n" , | sed -e 's/,$/°C/') | #(ip link show | grep LOWER_UP | grep -v lo > /dev/null && ip link show | grep LOWER_UP | grep -v lo | cut -f 2 -d " " | tr -d : | tr "\n" , | sed -e 's/,$//' || echo NIC) | ☀ #(TEMP=`cat /sys/devices/pci0000:00/0000:00:02.0/backlight/acpi_video0/brightness`; B=$(( (TEMP * 100) /10 )); echo $B) | ♪ #(exec amixer get Master | egrep -o "[0-9]+%" | egrep -o "[0-9]*") | ♥#(acpi | tac | cut -d ',' -f 2) | %a %m-%d %H:%M'
