set -g status-right-length 200
set-option -g status-position top
set -g status-right '#(cd #{pane_current_path}; git rev-parse --abbrev-ref HEAD) #(cd #{pane_current_path}; git status --porcelain | wc -l) | #(~/git/wiki/profile/git-tmux.sh) | #(~/git/wiki/profile/tmux_tasks.sh) | #(~/git/wiki/profile/weather.sh) | #(ip link show | grep LOWER_UP | grep -v lo > /dev/null && ip link show | grep LOWER_UP | grep -v lo | cut -f 2 -d " "| tr -d : || echo NIC) | ☀ #(TEMP=`cat /sys/class/backlight/intel_backlight/brightness`; B=$(( (TEMP * 100) /852 )); echo $B) | ♪ #(exec amixer get Master | egrep -o "[0-9]+%" | egrep -o "[0-9]*") | ♥#(acpi | tac | cut -d ',' -f 2) | %a %m-%d %H:%M'
